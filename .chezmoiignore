README.md

# Znap modules
.config/zsh/znap/**

##
# Machine-local shell config — deliberately never in this repo.
#
# 99-local.zsh is picked up automatically by the rc.d/<->-*.zsh glob in
# .zshrc, so it needs no loader changes. Listed here so `chezmoi add` on the
# directory can't sweep it into the public repo by accident.
#
.config/zsh/rc.d/99-local.zsh
.config/zsh/zshenv.local

# Generated by ~/.config/hypr/theme/generate.lua — never hand-edit or track
.config/hypr/colors.lua
.config/hypr/hyprtoolkit.conf
.config/hypr/hyprlock-colors.conf
.config/hypr/theme/palette.env
.config/nvim/lua/palette.lua
.config/ghostty/colors
.config/quickshell/generated/**
.config/gtk-3.0/gtk.css
.config/gtk-3.0/settings.ini
.config/gtk-4.0/gtk.css
.config/gtk-4.0/settings.ini
.Xresources

# AI
**/.claude/

# User space
**/.luarc.json

{{ if ne .chezmoi.os "linux" -}}
##
# Linux-only surface.
#
# Hyprland, quickshell and flatpak have no macOS equivalent, so everything
# under them is skipped entirely off Linux. The nvim/ghostty/zsh/starship
# configs are deliberately NOT in this list — they are shared across both
# machines and carry their own per-OS branches inside templates.
#
.config/hypr
.config/hypr/**
.config/quickshell
.config/quickshell/**
.var
.var/**
.local/share/wallpapers
.local/share/wallpapers/**
{{ end -}}
